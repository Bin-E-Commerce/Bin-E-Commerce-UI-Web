import { HomePageContent } from './_features/home/HomePageContent';

// Giữ route homepage mỏng để toàn bộ UI và logic dữ liệu nằm trong feature home chuyên biệt.
export default function HomePage() {
    return <HomePageContent />;
}
